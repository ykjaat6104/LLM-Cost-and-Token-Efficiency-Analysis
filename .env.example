# ── FREE TIER keys (available at no cost) ────────────────────────────────────
GROQ_API_KEY=gsk_...          # https://console.groq.com/keys
GEMINI_API_KEY=AIza...        # https://aistudio.google.com/app/apikey
CEREBRAS_API_KEY=csk-...      # https://cloud.cerebras.ai

# ── PAID TIER keys (add when you have them) ───────────────────────────────────
OPENAI_API_KEY=sk-...         # https://platform.openai.com/api-keys
ANTHROPIC_API_KEY=sk-ant-...  # https://console.anthropic.com/settings/keys

# ── Tier Selection ────────────────────────────────────────────────────────────
# "free"  → Groq, Gemini (free flash models), Cerebras
# "paid"  → OpenAI, Anthropic, Gemini Pro
# "all"   → run every configured model
ACTIVE_TIER=free

# ── Demo Mode ─────────────────────────────────────────────────────────────────
# true  → simulate API responses (no keys needed, no usage)
# false → call real APIs using the keys above
DEMO_MODE=true
